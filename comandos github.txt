comandos

clear: limpar o terminal
configura username: 
git config --global user.name "nomedousuario"

configura email
git config --global user.email "email"

mkdir   nomeprojeto
cd nomeprojeto

git init   (faz o diretorio se transformar em um repositorio)
code .     (comando do vs code para caminho do arquivo)

ls (lista os arquivos)
git add .

1.Repository
git init ( criar um novo repositório local)
git clone (clonar um repositório remoto)

2. commit 
git add file (adicionar arquivos ao stage)
git add . (para adicionar todos)

arquivos modificados:
fazer um commit: git commit -m "mensagem do commit"

3. push
git push origin <nomedabranch>

<nomedabranch> (main)


